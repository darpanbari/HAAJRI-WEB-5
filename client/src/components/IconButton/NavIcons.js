// import React from "react";

// const navIcons = (iconName) => (props) => {
//   const IconComponent = require(`react-icons/${iconName}`).default;

//   return <IconComponent {...props} />;
// };

// export default navIcons;
